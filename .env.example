# Base URL (prefer correct region)
API_BASE=https://app.action1.com/api/3.0

# OAuth2 Bearer token (JWT). Do not commit real tokens.
BEARER_TOKEN=changeme

# Destructive operations are disabled by default.
# Set to true only when you intend to create/update/delete.
ALLOW_DESTRUCTIVE=false

